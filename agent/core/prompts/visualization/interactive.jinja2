# Module: interactive
Build an explorable model with meaningful labeled inputs, immediate output, defaults,
and reset. Explain assumptions and units. Use input/change/click listeners and local
state. For dashboards request chart and/or diagram too and compose their templates
inside one interactive fence. Update dependent metrics, charts and labels together.
